#include <iostream>
#include <vector>
#include <algorithm>

using namespace std;

void heapSort(vector<int> &arr, bool isIncreasing)
{
}

int main()
{
    vector<int> arr = {10, 20, 30, -2, -3, -4, 5, 6, 7, 8, 9, 22, 11, 13};
    heapSort(arr, true);

    return 0;
}